<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="24dp"
    app:cardCornerRadius="28dp"
    app:cardElevation="6dp"
    app:cardBackgroundColor="@color/dialog_bg" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:gravity="center_horizontal">

        <ImageView
            android:id="@+id/ivIconoAlerta"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginTop="8dp"
            android:src="@android:drawable/ic_dialog_alert"
            app:tint="@color/dialog_azul_confirmar" />

        <TextView
            android:id="@+id/tvTitulo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="¡Base de datos no encontrada!"
            android:textColor="@color/texto_titulo_cita"
            android:textSize="18sp"
            android:textStyle="bold"
            android:gravity="center" />

        <TextView
            android:id="@+id/tvMensaje"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:layout_marginHorizontal="8dp"
            android:text="La base de datos que ha indicado no existe, quiere crearla?"
            android:textColor="@color/texto_subtitulo_cita"
            android:textSize="14sp"
            android:gravity="center"
            android:lineSpacingMultiplier="1.1" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:layout_marginBottom="8dp"
            android:orientation="horizontal">

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/btnNo"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:layout_marginEnd="10dp"
                android:text="CANCELAR"
                android:textSize="13sp"
                android:textStyle="bold"
                android:textColor="@color/dialog_btn_cancelar_text"
                android:backgroundTint="@color/dialog_btn_cancelar_bg" />

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/btnSi"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:layout_marginStart="10dp"
                android:text="CONFIRMAR"
                android:textSize="13sp"
                android:textStyle="bold"
                android:textColor="@android:color/white"
                android:backgroundTint="@color/dialog_azul_confirmar" />

        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>